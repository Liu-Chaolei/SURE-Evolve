# Prediction Generation v2

Generate into the fixed local result staging root with
`scripts/generate_predictions_via_server.py`, then commit the immutable run
with `scripts/finalize_inference_run.py`. See [AGENTS.md](../AGENTS.md).
