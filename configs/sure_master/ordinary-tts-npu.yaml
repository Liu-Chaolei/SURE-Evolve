# Complete ordinary + XLab configuration. Full runs are opt-in; use probe_task.py for lightweight checks.
sure:
  root: ${SURE_EVAL_ROOT}
  pythonpath: ${SURE_EVAL_ROOT}/src
  task_cards_path: ./playground/sure_master/task_cards/sure_tasks.yaml
  task_id: tts_zh_cer
  device: cpu
  cache_dir: null
  validate_env: false
  require_base_model: true
  workspace_cleanup:
    enabled: true
    on_success: true
    on_failure: false
    remove_models: true
    remove_working: true
    remove_runtime_cache: true
    compact_metric_logs: true
    keep_log_tails: true
    log_tail_bytes: 65536
  execution_mode: local
  search_strategy: ordinary
  coordinator:
    local_gpu_policy: disabled
  remote_training:
    enabled: false
  initial_solution_path: playground/sure_master/baselines/task_baseline.py
  execution_env:
    SURE_REQUIRE_CANDIDATE_CHANGES: '1'
    TOKENIZERS_PARALLELISM: 'false'
    WANDB_MODE: offline
  base_models:
    tts_zh_cer:
      model_id: f5tts_v1_base
      framework: f5-tts
      usage_policy: required
      required_paths:
        root: base_model/root
      source_paths:
        root: ${SURE_F5_ROOT}
      python: ${SURE_WORKER_PYTHON}
      prompt_guidance: Use SURE_TASK_WRAPPER with --action infer|fine_tune|arch and
        --parameters-json; model/data dependencies are supplied by the adapter.
  runtime:
    accelerator: npu
    devices:
    - ${SURE_DEVICE}
    python: ${SURE_WORKER_PYTHON}
    precision: fp32
    cpu_threads: 4
  metric_gpu:
    enabled: false
  preflight:
    enabled: true
  require_training_candidate: true
  execution_contract:
    initialization: fixed baseline for training; round best for inference
    training_seed: 42
    precision: fp32
    world_size: 1
    candidate_entrypoint: SURE_TASK_WRAPPER
    constraints:
    - Only change registered wrapper parameters.
    - Train only on train; never access holdout during search.
    - Frozen evaluation never trains.
  adapter: tts.f5tts
  require_model_artifact: true
  source_snapshot:
    enabled: true
  metric_runtime:
    python: ${SURE_METRIC_PYTHON}
    device: cpu
  datasets:
    train:
      manifest: ${SURE_TTS_DATA}/train/manifest.jsonl
    train_validation:
      manifest: ${SURE_TTS_DATA}/train_validation/manifest.jsonl
    search:
      manifest: ${SURE_TTS_DATA}/search/manifest.jsonl
    selection:
      manifest: ${SURE_TTS_DATA}/selection/manifest.jsonl
    holdout:
      manifest: ${SURE_TTS_DATA}/holdout/manifest.jsonl
  task:
    language: zh
    resources:
      source: ${SURE_F5_ROOT}
      checkpoint: ${SURE_F5_CHECKPOINT}
      vocab: ${SURE_F5_VOCAB}
      vocoder: ${SURE_VOCODER_ROOT}
    training:
      manifest: ${SURE_TTS_DATA}/train/metadata.csv
      max_steps: 1000
      learning_rate: 3.0e-06
      effective_batch_size: 8
      seed: 42
    inference:
      nfe_step: 32
      cfg_strength: 2.0
      sway_sampling_coef: -1.0
      speed: 1.0
      remove_silence: false
      text_cleanup: none
      max_chunk_chars: 300
      min_chunk_chars: 40
      target_rms: 0.1
      cross_fade_duration: 0.15
max_research_rounds: 2
llm:
  openai:
    provider: openai
    model: ${SURE_AGENT_MODEL}
    api_key: ${OPENAI_API_KEY}
    base_url: ${OPENAI_BASE_URL}
    temperature: 1
    max_tokens: 32768
    timeout: 3600
  default: openai
agents:
  prefetch:
    llm: openai
    max_turns: 10
    tools:
      builtin: []
    context:
      max_tokens: 128000
      truncation_strategy: latest_half
      preserve_system_messages: true
      preserve_recent_turns: 5
    system_prompt_file: prompts/prefetch_system_prompt.txt
    user_prompt_file: prompts/prefetch_user_prompt.txt
  draft:
    llm: openai
    max_turns: 10
    tools:
      builtin: []
    context:
      max_tokens: 128000
      truncation_strategy: latest_half
      preserve_system_messages: true
      preserve_recent_turns: 5
    system_prompt_file: prompts/draft_system_prompt.txt
    user_prompt_file: prompts/draft_user_prompt.txt
  debug:
    llm: openai
    max_turns: 10
    tools:
      builtin: []
    context:
      max_tokens: 128000
      truncation_strategy: latest_half
      preserve_system_messages: true
      preserve_recent_turns: 5
    system_prompt_file: prompts/debug_system_prompt.txt
    user_prompt_file: prompts/debug_user_prompt.txt
  improve:
    llm: openai
    max_turns: 10
    tools:
      builtin: []
    context:
      max_tokens: 128000
      truncation_strategy: latest_half
      preserve_system_messages: true
      preserve_recent_turns: 5
    system_prompt_file: prompts/improve_system_prompt.txt
    user_prompt_file: prompts/improve_user_prompt.txt
  knowledge_promotion:
    llm: openai
    max_turns: 10
    tools:
      builtin: []
    context:
      max_tokens: 128000
      truncation_strategy: latest_half
      preserve_system_messages: true
      preserve_recent_turns: 5
    system_prompt_file: prompts/knowledge_promotion_system_prompt.txt
    user_prompt_file: prompts/knowledge_promotion_user_prompt.txt
  wisdom_promotion:
    llm: openai
    max_turns: 10
    tools:
      builtin: []
    context:
      max_tokens: 128000
      truncation_strategy: latest_half
      preserve_system_messages: true
      preserve_recent_turns: 5
    system_prompt_file: prompts/wisdom_promotion_system_prompt.txt
    user_prompt_file: prompts/wisdom_promotion_user_prompt.txt
session:
  type: local
  local:
    working_dir: ./runs/tts_npu
    timeout: 43200
    gpu_devices: none
    parallel:
      enabled: true
      max_parallel: 1
      split_workspace_for_exp: true
      set_asr_world_size: false
llm_output:
  show_in_console: true
  log_to_file: true
logging:
  level: INFO
  format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
  file: null
  console: true
xlab:
  enabled: true
  idea_generation:
    max_attempts: 8
  idea_provider:
    enabled: true
    preflight_command:
    - ${XLAB_PYTHON}
    - ${XLAB_ROOT}/xlab/skills/sure_master/scripts/xlab_idea_client.py
    - --check-survey
    - ${XLAB_SURE_SURVEY_PATH}
    command:
    - ${XLAB_PYTHON}
    - ${XLAB_ROOT}/xlab/skills/sure_master/scripts/xlab_idea_client.py
    timeout_seconds: 14400
    environment:
      XLAB_SURE_RUN_ROOT: ${SURE_EVOLVE_ROOT}/runs/xlab_ideas
      XLAB_SURE_SURVEY_PATH: ${XLAB_SURE_SURVEY_PATH}
